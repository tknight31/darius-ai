import React from "react";

const Footer = () => {
  return (
    <footer className="text-xs row-start-3 flex gap-6 flex-wrap items-center justify-center ">
      not actually associated with Atlanta though
    </footer>
  );
};

export default Footer;
